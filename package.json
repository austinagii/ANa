{
  "name": "gibber",
  "version": "0.0.1",
  "dependencies": {
    "npx": "^10.2.2"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "@commitlint/config-conventional": "^17.4.2",
    "husky": "^8.0.3"
  },
  "scripts": {
    "prepare": "husky install",
    "start-ui": "cd gibber-ui && npm run dev"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
