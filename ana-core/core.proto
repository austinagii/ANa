syntax = "proto3";

package core;

service Model {
    rpc completePrompt (Prompt) returns (PromptCompletion) {};
}

message Prompt {
    string text = 1;
}

message PromptCompletion {
    string text = 1;
}